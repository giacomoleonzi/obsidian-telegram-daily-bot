version: "3.8"

services:
  obsidian-telegram:
    build:
      context: .
    image: obsidian-telegram:arm64
    container_name: obsidian-telegram
    restart: unless-stopped
    platform: linux/arm64
    env_file:
      - ./config/.env
    volumes:
      - ./vault:/vault

